<!DOCTYPE html>
<html lang="en">

<%- include('../include/head.ejs') %>

    <body>
        <%- include('../include/navbar.ejs') %>
            <h1>
                <%=  editing  %>
            </h1>
            <main>
                <form class="product-form" action="/admin/<% if (editing) { %>edit-product<%} else {%>add-product<%}%>" method="POST">
                    <div class="form-control">
                        <label for="title">Title</label>
                        <input type="text" name="title" id="title" value="<%= editing ? product.title : '' %>" required>
                    </div>
                    <div class="form-control">
                        <label for="imageUrl">imageUrl</label>
                        <input type="text" name="imageUrl" id="imageUrl" value="<% if (editing) { %><%=product.imageUrl%> <%}%>" required>
                    </div>
                    <div class="form-control">
                        <label for="price">price</label>
                        <input type="number" name="price" id="price" min="0" value="<% if (editing) { %><%= product.price %><%}%>" required>
                    </div>
                    <div class="form-control">
                        <label for="description" >description</label>
                        <textarea type="text" name="description" id="description" required><%= editing ? product.description : '' %></textarea>
                    </div>
                    <%if (editing) {%>
                        <input type="hidden" name="productId" value="<%= product._id%>">
                    <%}%>
                    <button type="submit">
                        <% if (editing) {%>
                            Update Product
                        <%} else {%>
                            Add Product
                        <%}%>
                    </button>
                </form>
            </main>
    </body>

</html>