<!DOCTYPE html>
<html lang="en">

<%- include('../include/head.ejs') %>

<body>
    <%- include('../include/navbar.ejs') %>
    <% if (orders.length <= 0) {%>
    <h1>Nothing There</h1>
    <%} else {%> 
        <ul>
            <% orders.forEach(order =>  {%>
                <li>
                    <h1># <%= order.id%></h1>
                    <ul>
                        <% order.product1s.forEach(item => {%>

                        <li><%= item.title%>(<%= item.orderItem.quantity %>)</li>
                        <%})%>
                    </ul>
                </li>
            <%})%>
        </ul>
    <% }%>